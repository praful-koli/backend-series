==> Authentication
    => register (1. user ka data save krna. 2. token user ko dena)
    => login (1.check user & password is valid 2. create a token and save in the client cookie)
    => logout (token blacklisting)
    => [OTP based registration]

==> post
    => create
    => can see the feed
    => like posts (collection types)
    => save post

==> users
    => followers
    => following